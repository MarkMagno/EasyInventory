runtime: python39
entrypoint: entrypoint: gunicorn -b :$PORT --timeout 120 --workers 2 EasyInventory.wsgi:application

handlers:
- url: /static
  static_dir: static/

env_variables:
    DJANGO_SECRET_KEY: "your-secret-key"
    ALLOWED_HOSTS: "your-domain.com"
